#!/bin/python3

import math
import os
import random
import re
import sys
from typing import Dict, Tuple, Optional, List

class Smatix:
   
    def __init__(self, sparsified,line_len,row_num):
        self.sparsified = sparsified
        self.line_len = line_len
        self.row_num = row_num
    
        
    def add():
    def subtract():
    def display():

# I would provide function implementation as list of lists.
# Shape of the matrix based on line_len & row_num